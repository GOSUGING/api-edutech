package com.edutech.notificaciones.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.edutech.notificaciones.model.TipoAsunto;

public interface TipoAsuntoRepository extends JpaRepository <TipoAsunto, Integer>{
}
